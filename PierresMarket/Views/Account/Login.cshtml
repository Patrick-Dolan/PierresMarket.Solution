@{
  Layout = "_Layout";
}

@using PierresMarket.ViewModels

@model LoginViewModel

<div class="d-flex">
  <div class="col-md-5 card mx-auto shadow-lg p-3 mb-5 bg-body rounded">
    @using (Html.BeginForm())
    {
      <h2 class="text-center">Sign in</h2>
      <hr>
      <div class="mb-3">
        @Html.LabelFor(m => m.Email, new { @class = "form-label"})
        @Html.TextBoxFor(m => m.Email, new { @class = "form-control", required="required"})
      </div>
      <div class="mb-3">
        @Html.LabelFor(m => m.Password, new { @class = "form-label"})
        @Html.PasswordFor(m => m.Password, new { @class = "form-control", required="required"})
      </div>
      <button type="submit" class="btn btn-success btn-block mb-3">Log in</button>
      <p class="text-muted text-center">New to Pierre's? @Html.ActionLink("Create an account", "Register")</p>
    }
  </div>
</div>
